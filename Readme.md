# SVGR para convertir iconos svg a compomnentes de react

npm install --save-dev @svgr/cli

Para que el svgr.config.js funcionara se tiene q borrar el 'type:module' del package.json al menos de mommento
